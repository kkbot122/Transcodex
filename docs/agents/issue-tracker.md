# Issue Tracker

GitHub Issues in `kkbot122/Transcodex` is the canonical project issue tracker.

Publish buildable specifications and implementation tickets as GitHub issues. Keep durable design documents in the repository and link them from the corresponding issue. Use blocking relationships in issue descriptions when work is decomposed into multiple tickets.
