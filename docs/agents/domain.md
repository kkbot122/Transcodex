# Domain Documentation

Transcodex uses one repository-wide domain context:

- The canonical glossary is `CONTEXT.md` at the repository root.
- Hard-to-reverse architectural decisions belong in `docs/adr/`.
- Specs and implementation notes must use glossary terms consistently and must not turn `CONTEXT.md` into an implementation document.
